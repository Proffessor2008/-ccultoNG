ØccultoNG Pro v2.1.0 🔐
Профессиональный инструмент стеганографии с расширенными методами скрытия данных в изображениях и аудио, теперь с пакетной обработкой.​

[![Python 3.8+](https://img.shields.io/badge/Python-3.8%2[![License](https://img.shields.io/badge/License-Propriet[![Version](https://img.shields.io/badge/Version-2.1.[![Status](https://img.shields.io/badge/Status-Production(https://github.com/Proffessor2008/stegoprohttps://img.shields.io/badge/Platform-Windows%20%7C%20macOS%20%7C%20Linux(https://github.com/Proffessor2008/stegopro Содержание

Описание

Что нового в 2.1.0

Ключевые особенности

Поддерживаемые методы

Установка

Быстрый старт

Пакетная обработка

Использование

Архитектура

Расширенные возможности

Безопасность

Часто задаваемые вопросы

Вклад в проект

Лицензия

Автор и контакты

🎯 Описание
ØccultoNG Pro — инструмент для скрытия конфиденциальных данных внутри изображений и аудиофайлов (PNG, BMP, TIFF, TGA, JPEG, WAV) с использованием классических алгоритмов стеганографии и вспомогательной криптографии. Приложение ориентировано на практическое использование: учебные задачи, CTF, исследования в области стеганографии и кибербезопасности.​

Программа зарегистрирована в Роспатенте и развивается как закрытый, но технически ориентированный продукт, с фокусом на понятной архитектуре и расширяемости.

🆕 Что нового в 2.1.0
Версия 2.1.0 — это минорный релиз поверх v2.0.0, сфокусированный на пакетной обработке и улучшении удобства работы с несколькими файлами.

Основные изменения:

✅ Добавлена пакетная обработка (batch):

обработка сразу нескольких контейнеров (список файлов);

единые настройки метода, пароля и параметров для всего батча;

последовательная обработка с отображением прогресса.

✅ Новая логика в GUI:

отдельный режим/вкладка для пакетной обработки (если реализовано в интерфейсе);

отображение статуса для каждого файла: «успех / ошибка».

✅ Логирование:

запись результатов пакетных операций в HistoryLog (успехи/ошибки для каждого файла);

улучшенное сообщение об ошибках для батча.

✅ Небольшие улучшения:

рефакторинг части кода вокруг AdvancedStego/основного окна;

косметические правки текста, подсказок и сообщений в интерфейсе (если внесены).

Важно:
В этой версии не добавлялись новые методы стеганографии, не менялись криптографические примитивы и не расширялся список поддерживаемых форматов — главным фокусом была именно автоматизация работы с несколькими файлами.

✨ Ключевые особенности
🎨 Интерфейс
✅ 9 профессиональных тем (Тёмная, Светлая, Космос, Океан, Лес, Неонка, Закат, Киберпанк, Матовая)

✅ Drag-and-drop для контейнеров и данных

✅ Предпросмотр изображений со статистикой

✅ Прогресс-бар при обработке крупных файлов и батчей

✅ Горячие клавиши (Ctrl+O, Ctrl+H, Ctrl+E и др.)

✅ История последних файлов и HistoryLog

🔐 Функциональность
✅ 6 методов стеганографии: LSB, Adaptive-Noise, AELSB (+Hamming), HILL-CA, JPEG DCT, WAV LSB

✅ Автоматическое определение метода при извлечении (по заголовку/магическим байтам)

✅ Поддерживаемые форматы: PNG, BMP, TIFF, TGA, JPEG, WAV

✅ CRC32 + Hamming(7,3) для контроля целостности и восстановления одиночных ошибок

✅ PBKDF2 + Base64 для защиты пароля

✅ Сжатие данных (zlib) перед встраиванием

📊 Аналитика и логирование
✅ HistoryLog с базовой информацией об операциях

✅ Статистика по методам/форматам (в пределах реализованного функционала)

✅ Возможность использовать логи для отладки и анализа экспериментов

🤖 Умные возможности
✅ SmartAssistant с контекстными подсказками в UI

✅ Автоматическое резервное копирование (в реализованных сценариях)

✅ Рекомендации по выбору метода в зависимости от типа контейнера

✅ Export/Import настроек через JSON-конфиг

🔬 Поддерживаемые методы
Метод	Вместимость	Скрытность	Устойчивость	Формат
LSB	Максимальная	Слабая	Слабая	PNG/BMP/TIFF/TGA
Adaptive-Noise	Средняя	Средняя	Средняя	PNG/BMP/TIFF/TGA
AELSB + Hamming	Средняя	Средняя	Высокая ⭐	PNG/BMP/TIFF/TGA
HILL-CA	Средняя	Высокая ⭐	Средняя	PNG/BMP/TIFF/TGA
JPEG DCT	Средняя	Высокая ⭐	Средняя	JPEG
WAV LSB	Максимальная	Слабая	Слабая	WAV
Реальная вместимость зависит от размера контейнера, метода и параметров компрессии.

📥 Установка
Системные требования
bash
✓ Python 3.8 или выше
✓ 4GB RAM (для работы с большими изображениями/батчами)
✓ Windows 10+, macOS 10.14+ или Linux (любая современная сборка)
Пошаговая установка
bash
git clone https://github.com/Proffessor2008/stegoproexp.git
cd stegoproexp

# (рекомендуется) виртуальное окружение
python -m venv venv          # или python3 -m venv venv
venv\Scripts\activate        # Windows
# source venv/bin/activate   # macOS/Linux

pip install -r requirements.txt

python stegoproexp.py
requirements.txt:

text
Pillow>=9.0.0
numpy>=1.21.0
scipy>=1.7.0
numba>=0.55.0
opencv-python>=4.5.0
tkinterdnd2>=0.3.0
🚀 Быстрый старт
Скрытие данных (один файл)
Запустите приложение: python stegoproexp.py

Выберите метод (LSB / HILL-CA / JPEG DCT и т.д.)

Перетащите контейнер (PNG/JPEG/WAV) в окно

Выберите/перетащите данные (текстовый файл, бинарный файл и т.п.)

Задайте пароль (опционально)

Нажмите «Скрыть» и сохраните результат

Извлечение данных (один файл)
Перейдите на вкладку «Извлечение»

Перетащите контейнер со скрытыми данными

Программа попытается определить метод автоматически

Введите пароль (если использовался)

Нажмите «Извлечь» и сохраните/просмотрите данные

📦 Пакетная обработка
В v2.1.0 добавлена возможность обрабатывать несколько файлов подряд с общими настройками стеганографии.

Пакетная обработка через GUI
Типичный сценарий:

Откройте режим/вкладку пакетной обработки (Batch)

Добавьте несколько контейнеров:

через drag-and-drop группы файлов;

или через диалог выбора (Multi-select).

Выберите:

операцию: скрытие или извлечение;

метод (для скрытия);

пароль и дополнительные параметры.

Запустите пакетную обработку.

Наблюдайте прогресс:

общий прогресс;

статус по каждому файлу (успех/ошибка).

После завершения откройте лог/папку с результатами.

Ограничения v2.1.0:

Пакетная обработка выполняет операции последовательно, без параллелизма.

Настройки (метод, пароль и т.п.) общие для всех файлов в батче.

При ошибке в одном файле обработка остальных продолжается (если так реализовано в коде).

Пакетная обработка из кода (пример)
python
from stegoproexp import AdvancedStego

files = [
    ("photo1.png", "out1.png"),
    ("photo2.png", "out2.png"),
    ("photo3.png", "out3.png"),
]

data = b"Secret data for all"
password = "BatchPassword123"

for src, dst in files:
    AdvancedStego.hide_lsb(
        src,
        data,
        password,
        dst,
        progress_callback=lambda p: print(f"{src}: {p}%")
    )
Если у тебя в stegoproexp.py есть отдельный класс/обёртка для батча (например, BatchProcessor), сюда можно будет позже добавить реальный API-пример именно с этим классом.

💻 Использование (API)
API в целом соответствует v2.0.0, все существующие методы остались без обратимо несовместимых изменений.

python
from stegoproexp import AdvancedStego, JPEGStego, ImageProcessor

# Пример LSB
AdvancedStego.hide_lsb("photo.png", b"Secret", "pass", "stego.png")
data = AdvancedStego.extract_lsb("stego.png", "pass")

# Пример JPEG DCT
JPEGStego.hide_dct("photo.jpg", b"Secret", "pass", "stego_photo.jpg")
data = JPEGStego.extract_dct("stego_photo.jpg", "pass")

# Оценка вместимости
capacity = ImageProcessor.get_capacity_by_method(
    total_pixels=1920*1080*3,
    method="lsb",
)
print("LSB capacity:", capacity)
🏗️ Архитектура
Основная структура проекта и ключевые классы остаются такими же, как в v2.0.0, с добавлением логики для батча внутри основного файла приложения:

AdvancedStego — методы для LSB, Adaptive-Noise, AELSB, HILL-CA

JPEGStego — работа с JPEG DCT

AudioStego — стеганография для WAV

ImageProcessor — вычисление вместимости, предобработка

ThemeManager — темы интерфейса

HistoryLog — логирование операций

SmartAssistant — подсказки и рекомендации

(опционально) модуль/класс для batch-логики, если выделен в коде

🔧 Расширенные возможности
Горячие клавиши (Ctrl+O, Ctrl+H, Ctrl+E, Ctrl+S, Ctrl+Z, Ctrl+Y, Ctrl+C, F5, F1)

Переменные окружения (STEGO_DEBUG, STEGO_LOG_DIR, STEGO_MAX_FILE_SIZE)

Экспорт/импорт настроек через JSON-конфиг stego_settings_*.json

HistoryLog/статистика для анализа использования программы

🔐 Безопасность
PBKDF2-SHA256 с большим числом итераций для защиты паролей​

CRC32 и Hamming(7,3) для обнаружения и частичного исправления ошибок

Сжатие и кодирование перед встраиванием для повышения плотности и устойчивости

Программа не претендует на замену сертифицированных криптосредств, но даёт практический инструментарий для обучения, исследований и демонстрации принципов стеганографии.

❓ Часто задаваемые вопросы
Секция FAQ по смыслу совпадает с v2.0.0 (выбор метода, вместимость, влияние редактирования, обратная совместимость), с добавлением вопроса про пакетную обработку:

Q: Что умеет пакетная обработка в 2.1.0?
A: Пакетный режим позволяет обрабатывать список файлов с общими настройками (метод, пароль, параметры), последовательно проходясь по каждому контейнеру и сохраняя результат, что ускоряет работу с большими наборами данных.

🤝 Вклад в проект
новые методы стеганографии;

улучшения UI/UX (особенно batch-режима);

оптимизация производительности (Numba, возможное GPU-ускорение);

тесты для сценариев батч-обработки.

📝 Лицензия
ØccultoNG Pro распространяется по модели Proprietary + Academic License (личное, академическое и коммерческое использование на условиях, описанных в LICENSE).

👨‍💻 Автор и контакты
MustaNG — разработчик ØccultoNG Pro, школьник 11 класса из Махачкалы, ориентирующийся на поступление в РТУ МИРЭА и кибербезопасность.

Email: tudubambam@yandex.ru

GitHub: Proffessor2008
